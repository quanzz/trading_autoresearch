[project]
name = "trading-autoresearch"
version = "0.1.0"
description = "Autonomous trading strategy optimization via backtesting"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8.0",
    "numpy>=2.0.0",
    "pandas>=2.0.0",
    "pyyaml>=6.0",
]
